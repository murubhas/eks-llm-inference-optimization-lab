# AIPerf Test Fixture

`profile_export_aiperf.sample.json` is a minimal, sanitized AIPerf `0.10.0`
export used to test the summary generator. It contains no endpoint URL, raw CLI
command, account identifier, or absolute artifact path.

The numbers are representative test data. Use the checked-in sanitized result
record for workshop interpretation, not this fixture.
